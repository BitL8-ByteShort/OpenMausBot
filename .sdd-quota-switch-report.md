# Quota-switch: continue on another engine — implementation report

Branch: `feat/quota-switch` (worktree `/Users/omkar/Desktop/openmaus/OpenGrokBot-quota`), off `origin/main` 536b7893.

## What was built

**Part 1 — the quota card (`server/quota-switch.ts`, new module)**

- `maybeRaiseQuotaCard(deps, bot, threadId, errorMessage)` — the single entry
  point server/index.ts calls from `case "runtime.error":`. It owns the
  quota decision itself (calls `classifyError({ text: errorMessage })` from
  `server/drivers/retry.ts` and bails unless `reason === "quota"`), then:
  - looks up `deps.instances()` (wired to `registry.instances()`), filters
    out the bot's current `modelSelection.instanceId`, caps at 3;
  - bails (returns `null`) if there are no alternatives, or if a card is
    already pending on this thread (`pendingQuotaThreadIds`);
  - pushes one harness-native options card via `store.appendMessage` —
    title `"<Current engine> is out of quota"`, subtitle asking to
    continue on another engine, options = alternative display names +
    `"Not now"`. Deliberately carries **no `card.tool`** — see below.
  - records the pending offer (`pendingQuotaCards`, keyed by `requestId`,
    and `pendingQuotaThreadIds`, keyed by `threadId`, for the one-card-per-
    thread rule) — same in-memory-only pattern as `peer-approval.ts`.

- `resolveQuotaSwitch(deps, requestId, message)` — called from both
  `/api/bots/:id/respond` and `/api/threads/:id/respond`, right next to the
  existing `resolvePeerComms(...)` intercepts, in the same
  `if (store.messagesFor(...).some(...) && resolveX(...))` shape. Returns
  `false` immediately if `requestId` isn't one of ours (so peer/provider
  cards keep routing normally). If it is:
  - settles the card synchronously (`answered: "answer"`,
    `answeredText: <chosen label or "Not now">` — matches how every other
    tool-less "question" card remembers its reply, per
    `server/index.ts`'s `answerRequest`);
  - if the message matched one of the offered engines, kicks off (fire-
    and-forget, `void`) the actual switch + re-dispatch;
  - "Not now" (or anything else — e.g. the card being dismissed with X)
    settles the card and does nothing further.
  - Returns `true` synchronously either way, so it slots into the same
    `if (...) return` chain `resolvePeerComms` already sits in without
    forcing the two respond endpoints to become any more `async` than they
    already are.

**Why the card carries no `tool`**: I read `src/lib/card-answer.ts` before
designing the wire shape. `answerResponse(card, answer)` branches on
`card.tool`: if set, EVERY non-"Deny" press collapses to `behavior: "allow"`
— fine for a 2-outcome permission card, but it would erase which engine was
picked. Leaving `tool` unset makes the client treat it as a question
(`ChatView.tsx`'s `m.card?.requestId && m.card.tool` → `ApprovalCard`,
otherwise → `OptionCard`), so `behavior` is always `"answer"` and the
pressed label reaches the server verbatim as `body.message`. That is the
only way a card offering more than two choices can carry which one was
picked.

**Picking a valid model on the target engine** — `pickSelection` reuses
`selectDefaultModelSelection` from `server/default-model-selection.ts`
verbatim, handing it a one-element array built from the chosen instance's
freshly-awaited `snapshot()`. If the instance is available it returns that
instance with its own default model; if not (torn down between the card
being raised and answered), it returns the empty selection
`selectDefaultModelSelection` already uses for "nothing to offer" — no new
"is this a valid model" logic was written.

**Switching + re-dispatch** — on a valid selection: `store.patchBot` +
`store.patchTask` (same two calls the `/api/bots/:id/model` PATCH endpoint
makes), an activity chip ("continuing on <Engine> after hitting quota"),
then the last user `kind:"text"` message on `store.activePath(threadId)` is
re-sent through `deps.dispatch(botId, threadId, text)` — wired in
`server/index.ts` to `startTurn(botId, text, { threadId })`, so the
re-launched turn gets the exact same admission/lifecycle path any other
message does, and `buildTurnContext`'s existing "fresh engine" replay
(`engineIsFresh`) automatically carries the prior history across, since
nothing here is new — the feature this task's Part 2 improves.

**Part 2 — tool history in the replay (`server/turn-context.ts`,
`server/index.ts`)**

- `formatToolActivityLine(tool: { name, ok? })` — new pure, exported helper
  in `server/turn-context.ts`. Formats `[ran: <name lowercased> — ok]` or
  `… — failed]` (missing `ok` defaults to "ok", matching how activity chips
  already treat an absent `ok`), truncated to 120 chars with a trailing `…`.

- `server/index.ts`'s `startTurn` now builds a second, parallel array,
  `replayTranscript`, right next to the existing `transcript` (around the
  original line ~3986 the task pointed at — the `filter/slice(-40)/map`
  block). It is identical to `transcript` except its filter also admits
  `kind: "activity"` messages that carry a `tool`, and its `.map` renders
  those as `formatToolActivityLine(m.tool)` instead of `transcriptText(...)`.
  It reuses the exact same `slice(-40)` cap, so tool lines cannot make the
  replay grow unbounded — they compete with text turns for the same 40
  slots rather than adding to them.
  - `transcript` (text-only) is **untouched** and still feeds
    `engineIsFresh(...)`, `buildRecoveryText(...)` (the resume-recovery
    fallback), and the driver's own `SendTurnInput.transcript` for
    `replaysNatively` drivers (further down in `startTurn`) — exactly the
    three places the task said must not change.
  - Only the `buildTurnContext({...})` call's `transcript` field was
    changed to `transcript: replayTranscript`. Since `buildTurnContext`
    only ever reads its `transcript` argument when `!resume` (rewind /
    fresh engine / external update), a resumed turn is provably unaffected
    — confirmed by a dedicated test.

## TDD evidence

**quota-switch.ts** — RED: temporarily replaced the file with a type-only
stub whose two exports throw `"not implemented"`, then ran
`pnpm vitest run server/quota-switch.test.ts`:

```
Test Files  1 failed (1)
     Tests  10 failed (10)
```//: # (all 10 failed with "Error: not implemented")

GREEN — restored the real implementation, same command:

```
Test Files  1 passed (1)
     Tests  10 passed (10)
```

**turn-context.ts** — RED: temporarily stubbed `formatToolActivityLine` to
throw, ran `pnpm vitest run server/turn-context.test.ts`:

```
Test Files  1 failed (1)
     Tests  6 failed | 14 passed (20)
```//: # (the 6 new tests failed; the 14 pre-existing ones were untouched and kept passing)

GREEN — restored the implementation, same command:

```
Test Files  1 passed (1)
     Tests  20 passed (20)
```

## Final gate (run in this order, as instructed)

1. `pnpm vitest run server/quota-switch.test.ts server/turn-context.test.ts server/peer-approval.test.ts`
   → `Test Files 3 passed (3)`, `Tests 45 passed (45)`.
2. `npx tsc -p tsconfig.server.json --noEmit` → no output, exit 0.
3. `pnpm lint` (oxlint --deny-warnings) → no output, exit 0.

## Files changed

- `server/quota-switch.ts` — new. The whole feature's decision logic:
  raising, settling, picking a model, switching, re-dispatching.
- `server/quota-switch.test.ts` — new. The 10 tests listed in the task,
  plus two more (`caps the offered engines at three plus Not now`,
  `frees the thread for a new card once the pending one is resolved`, and
  `does not switch when the chosen engine is no longer available`) that
  came up naturally while pinning down the edge cases.
- `server/turn-context.ts` — added `formatToolActivityLine` (pure,
  exported). No existing exports changed.
- `server/turn-context.test.ts` — added tests for the new helper and for
  `buildTurnContext` with tool lines mixed into its `transcript` input.
- `server/index.ts` — wiring only:
  - imports `maybeRaiseQuotaCard`, `resolveQuotaSwitch`, `QuotaSwitchDeps`
    from `./quota-switch.ts`, and `formatToolActivityLine` from
    `./turn-context.ts`.
  - one new `const quotaSwitchBus: QuotaSwitchDeps = {...}` beside the
    existing `approvalBus`.
  - one line in `case "runtime.error":` calling `maybeRaiseQuotaCard`.
  - one `resolveQuotaSwitch` intercept block in each of the two respond
    endpoints, mirroring the existing `resolvePeerComms` blocks exactly.
  - the `replayTranscript` construction plus one changed field
    (`transcript: replayTranscript`) in the `buildTurnContext` call inside
    `startTurn`.

## Self-review

- Verified by reading `src/lib/card-answer.ts` and `ChatView.tsx`/
  `OptionCard.tsx`/`store.tsx` that a tool-less options card really does
  round-trip the pressed label as `body.message` with `behavior: "answer"`
  — this was the one non-obvious wiring decision in Part 1, and I did not
  want to guess at it.
- Confirmed `registry.instances()` only returns *live* (non-shadow)
  instances, so "configured" alternatives are never a driver that failed to
  load; `pickSelection`'s use of `selectDefaultModelSelection` additionally
  guards against an instance that is live but not currently `available`
  (signed out, etc.) at the moment the user actually answers — covered by
  the "does not switch when the chosen engine is no longer available" test.
- Confirmed via `git diff` that Part 2's change touches exactly one call
  site (`buildTurnContext`'s `transcript` field) and leaves the other three
  readers of `transcript` (`engineIsFresh`, `buildRecoveryText`,
  `SendTurnInput.transcript` for `replaysNatively` drivers) untouched.
- The quota card and its switch/re-dispatch are scoped to `bot` (1:1
  threads) only, guarded by the existing `if (bot) ...` pattern already in
  that handler (group/room turns leave `bot` undefined) — matches the
  task's wording throughout ("the bot's thread"/"the bot's modelSelection")
  and avoids inventing group-specific quota-offer semantics nobody asked
  for.

## Concerns

- The re-dispatch's fire-and-forget error path (`quotaSwitchBus.dispatch`'s
  `.catch`) posts a plain activity chip on `startTurn` failure (mirroring
  `drainQueuedSends`'s own fallback) rather than surfacing a fresh card —
  if the re-dispatch itself fails (e.g. the newly-chosen engine is also
  down), the person sees an error chip but no obvious retry affordance
  beyond typing again. This matches the task's scope (no mention of a
  cascading second offer) but is worth a follow-up if it comes up in
  practice.
- No test exercises the actual `server/index.ts` wiring end-to-end (i.e. a
  real `runtime.error` event flowing through `bus.subscribe` into a raised
  card, or a real HTTP `/respond` call hitting `resolveQuotaSwitch`) —
  `server/index.ts` has no existing test harness for the giant
  `bus.subscribe` handler to hook into, and the task scoped tests to
  `quota-switch.test.ts` (against the module directly, via its injected
  `QuotaSwitchDeps`) and `turn-context.test.ts`. The wiring itself is
  intentionally thin (a handful of lines mirroring `resolvePeerComms`'s
  existing, already-tested-elsewhere pattern), which is the reason I did
  not add an index.ts-level test myself.

## Fix round 1

A review of the above found three Important issues plus two small items.
All five are fixed. Nothing else from the review was touched — boot-time
stale-card cleanup and the duplicated transcript block in `server/index.ts`
were both explicitly called out as deferred and left alone.

### Finding 1 — the card could offer an unavailable engine

`maybeRaiseQuotaCard` used to filter candidates only by
`instanceId !== currentInstanceId`; availability was checked later, only
once the user clicked. Fixed by checking `instance.snapshot()` for every
candidate **when the card is built**, keeping only those whose
`snapshot.state === "available"` — the exact bar
`selectDefaultModelSelection`'s own "pick anything available" fallback
already uses, so what's offered and what `pickSelection` will later accept
are provably the same test. This makes `maybeRaiseQuotaCard` `async`
(`Promise<Message | null>`); its one caller in `server/index.ts` now reads
`void maybeRaiseQuotaCard(...)` — raising the card was always fire-and-
forget from the runtime-event handler's point of view, so nothing else
about that call site changed.

The click-time check in `pickSelection`/`switchAndRedispatch` stays as the
second line of defence (state can still change while the card sits open),
but its failure is no longer silent: `switchAndRedispatch` now posts an
activity chip ("`<Engine>` is no longer available — nothing switched.")
before returning, for both "instance vanished from the registry entirely"
and "instance still configured but no longer available" (`pickSelection`
returning the empty selection).

Covering tests (`server/quota-switch.test.ts`):
- `never offers an engine that is unavailable when the card is built` —
  Codex unavailable → card options are `["Grok", "Not now"]`, no Codex.
- `raises no card when every alternative is unavailable`.
- `does not switch when the chosen engine goes unavailable between raise
  and answer, and says so` — Codex is available when the card is raised,
  goes unavailable before the answer arrives; asserts `modelSelection`
  stays on Claude, `dispatch` is never called, and a
  `"...no longer available..."` chip (`ok: false`) is posted.

### Finding 2 — the card exposed a free-text input

Per the ruling, the card stays non-blocking (the composer is deliberately
left open) — only the stray input was in scope. Added
`fixedOptions?: boolean` to `OptionCardData` in `server/store.ts`
("answers must be one of `options`; no free-text reply") and to its
duplicate client-side type in `src/state/store.tsx` (the frontend does not
import the server type — `npx tsc -b` confirmed this second copy exists and
needed the same field, or the frontend build fails). Set
`fixedOptions: true` on the quota card. In `src/components/OptionCard.tsx`
the free-text `<input>` now also requires `!card.fixedOptions` to render
(alongside the existing `!card.answered && !card.tool`).

Server-side, an answer that names none of the offered engines — including
a stray free-text POST that reaches `/respond` directly, bypassing the now-
hidden input — was already routed through the same `chosen ? ... : NOT_NOW`
branch as an explicit "Not now", so no server logic changed here; added a
test to pin that this is really true: `treats an answer that names none of
the offered engines exactly like 'Not now'`.

`ios/` and `android/` were not touched, per instruction. Their own card
renderer does not know about `fixedOptions` and may still show a free-text
box beside this card's buttons; typing into it and sending answers
something that matches none of `pending.alternatives`, which the server-
side fix above already handles safely (treated as "Not now" — nothing
switches, the card just settles). This is a real, if minor, phone
inconsistency worth a follow-up ticket, not fixed here.

### Finding 3 — re-dispatch could resend the wrong message, or none

Replaced the "scan the active path backwards for any `kind:"text"` message"
at *answer* time with a `userMessageId` captured at *raise* time. In
`server/index.ts`'s `runtime.error` case, right before calling
`maybeRaiseQuotaCard`, the handler now finds the last `role: "user"`
message on `store.activePath(event.threadId)` — this is unambiguously the
message the failing turn was answering to, since nothing else can have been
appended to the thread between dispatch and this failure — and passes its
id through. `maybeRaiseQuotaCard` threads it into the new
`PendingQuotaCard.userMessageId` field.

`switchAndRedispatch` now looks that exact message up by id (never re-scans
"the last user message") and resends its raw `.text`. Since an attached
image travels as a `<attached-image path="..." name="..."/>` transport tag
embedded directly in the message's own `text`
(`server/turn-images.ts`/`startTurn` parse it back out on the way in),
resending the captured message's `text` verbatim resends its attachments
too — no separate attachments field exists on a user message to carry.

If the captured message can no longer be found (or its text is empty) when
the user answers, `switchAndRedispatch` now posts **no** "continuing on
`<Engine>`" chip; instead it posts
`"switched to <Engine>, but the message that hit quota could not be found
to resend — send it again."` (`ok: false`) and dispatches nothing. The
switch itself (`patchBot`/`patchTask`) still happens in this case — there
is no reason to also refuse using the new engine for the next message the
person types by hand.

Covering tests:
- `re-dispatches an image-only original message, tag and all` — the
  captured message's whole text is the `<attached-image .../>` tag; asserts
  `dispatch` is called with that exact tag string.
- `resends the message captured at raise time, not a newer one sent while
  the card was open` — appends a second, unrelated user message after the
  card is raised but before it is answered (the composer stays open, so
  this is a real scenario); asserts the ORIGINAL message is resent, not the
  newer one.
- `when the original message can no longer be found, posts no 'continuing'
  chip and says so instead` — blanks the captured message's text to stand
  in for "gone"; asserts no "continuing on..." chip, a
  "...could not be found to resend..." chip (`ok: false`) instead, and no
  dispatch.
- `raises no chip and dispatches nothing when there was no user message to
  capture` — `userMessageId: undefined` end to end (a bot's own turn with
  no originating user message); asserts the switch still lands but nothing
  is dispatched and no chip is asserted either way (the "not found" chip
  path and this path share the same `!original?.text` guard).

### Small items

- `switchAndRedispatch` now checks `store.patchBot(...)`'s return value and
  returns immediately (no further writes) when it is `null` — a bot deleted
  between the card being raised and answered no longer has a thread to
  chip into. Covering test: `writes no chip when the bot was deleted before
  the switch could land` (deletes the bot, then answers the card; asserts
  `dispatch` is never called and the — now nonexistent — thread has no
  messages).
- Confirmed no boot-time stale-card cleanup was added, and the duplicated
  `transcript`/`replayTranscript` block in `server/index.ts` (from Part 2 of
  the original task) was left exactly as it was — both deferred per
  instruction.

### TDD evidence for round 1

RED — reverted `server/quota-switch.ts` to the pre-round-1 version (its
`maybeRaiseQuotaCard` was still sync, filtered only by instanceId, and
`switchAndRedispatch` still re-scanned at answer time), ran the round-1
test file against it:

```
$ pnpm vitest run server/quota-switch.test.ts
 Test Files  1 failed (1)
      Tests  7 failed | 11 passed (18)
```

The 7 failures were exactly the new/rewritten round-1 tests (availability-
at-build-time, the two "no longer available"/"could not be found" chip
tests, the "resends captured id not a newer message" test, the image-only
test, and the deleted-bot test); the 11 pre-existing tests kept passing
unmodified.

GREEN — restored the round-1 implementation, same command:

```
$ pnpm vitest run server/quota-switch.test.ts server/turn-context.test.ts server/peer-approval.test.ts
 Test Files  3 passed (3)
      Tests  53 passed (53)
```

### Final gate for round 1 (run in the required order)

1. `pnpm vitest run server/quota-switch.test.ts server/turn-context.test.ts server/peer-approval.test.ts`
   → `Test Files 3 passed (3)`, `Tests 53 passed (53)`.
2. `npx tsc -p tsconfig.server.json --noEmit` → no output, exit 0.
3. `pnpm lint` (oxlint --deny-warnings) → no output, exit 0.

Also ran, beyond the required gate, since this round touched a frontend
file (`src/components/OptionCard.tsx`) and its duplicated type
(`src/state/store.tsx`): `npx tsc -b` (the project's own `typecheck` script
component covering `src/`) → no output, exit 0.

### Files changed in round 1

- `server/quota-switch.ts` — availability filtering at card-build time,
  `userMessageId` threaded through `PendingQuotaCard`, visible failure
  chips on both the "no longer configured", "no longer available", and
  "message could not be found" paths, `fixedOptions: true` on the card, the
  `patchBot`-return-value guard.
- `server/quota-switch.test.ts` — updated every call site for the new
  `async` signature and the new `userMessageId` parameter; added the tests
  listed above.
- `server/store.ts` — added `OptionCardData.fixedOptions`.
- `src/state/store.tsx` — added the same field to the frontend's own copy
  of the card type (required for `tsc -b` to pass).
- `src/components/OptionCard.tsx` — the free-text input's render condition
  now also checks `!card.fixedOptions`.
- `server/index.ts` — the `runtime.error` case now captures
  `failedUserMessage` from `store.activePath(...)` before calling
  `maybeRaiseQuotaCard`, and the call is now `void`-prefixed (async).

## Fix round 2

A wider test run (`server/direct-coordination.e2e.test.ts`) caught a real
regression in Part 2's replay change that the narrower round-1 gate missed.

### What was actually wrong

The reported symptom was right (`PRIVATE_ENGINEERING_FACT_8347` missing
from the chief's replayed prompt after an edit forced a rewind) and the
prescribed fix (cap content and tool lines separately, content first) is
exactly what was needed — but the *first* pass at applying it did not fix
the failing test, because the real defect was more specific than "one
shared `slice(-40)`". I confirmed this by instrumenting `server/index.ts`
with a temporary debug line and running the failing test in isolation
(spawns a real child-process server; its stdout log lands under
`$TMPDIR/openmausbot-verification-evidence/`): the thread in question had
only 5–7 messages total, nowhere near the 40-message cap, yet the report
still went missing. The actual message never got evicted — it got
**misclassified**.

A teammate's returned-report receipt (`server/index.ts`'s `report:`
room-handoff callback, ~line 2289) is stored as `kind: "activity"` with
`roomRequest: { phase: "result" }` **and** a `tool` chip, all on the same
message — it is simultaneously "content" (the plain text-only `transcript`
has always kept it via the `roomRequest?.phase === "result"` arm of its
filter) and "tool-bearing" (it has a `.tool` field). My round-1 mapping
function checked `m.kind === "activity" && m.tool` *before* checking
`roomRequest?.phase === "result"`, so this one message — despite being
correctly selected into the content window — got rendered as a generic
`formatToolActivityLine(m.tool)` "[ran: ...]" line instead of the actual
`teammateReportContext(...)` report text. The fact that it also happened to
sit inside a small thread with no eviction pressure at all is what proved
the cap-eviction theory alone was insufficient — the bug was in
classification, not capacity.

### The fix

1. Extracted the whole selection algorithm out of `server/index.ts` into a
   new pure, exported function in `server/turn-context.ts`:
   `selectReplayLines<T extends ReplayCandidate>(messages)`, plus two named
   constants, `REPLAY_CONTENT_CAP = 40` and `REPLAY_TOOL_LINE_CAP = 20`.
   `ReplayCandidate` is `{ isContent: boolean; hasTool: boolean }` — a
   caller-computed classification per message, not the raw `Message` shape,
   so the function has zero dependency on `server/store.ts` or any
   rendering helper and is trivially fixture-testable.
2. The algorithm: select content first (`isContent`), capped at
   `REPLAY_CONTENT_CAP`, identical to the plain text-only `transcript`'s own
   filter+cap — this cannot regress, since it is now the literal same
   selection. Record where that kept window starts. Add tool lines
   (`hasTool && !isContent`) only from that starting index onward, capped
   separately at `REPLAY_TOOL_LINE_CAP` (most recent kept). Merge and
   re-sort by original index so chronological order survives. **The
   `!isContent` guard on the tool-line filter is the actual fix**: a
   message already selected as content is never eligible to be reselected
   (or misrendered) as a tool line, however many fields it happens to
   carry.
3. `server/index.ts` now calls `selectReplayLines`, mapping each
   `activeMessages` entry to a `ReplayCandidate` (`isContent`/`hasTool`
   computed from the real `Message`), then maps the returned
   `{ item, line }` list into `{ role, text }` — the `line` tag decides
   rendering (`"tool"` → `formatToolActivityLine`, else the existing
   `roomRequest`/`transcriptText` branch), not a second look at the
   message's own fields. `skipTranscript` exclusion still happens in
   `server/index.ts` (filtered out of the array before it is handed to
   `selectReplayLines`) — the pure selector doesn't need to know about it.
   The plain text-only `transcript` variable is completely untouched.

### Covering tests (`server/turn-context.test.ts`, new `describe("selectReplayLines", ...)`)

- `keeps every content message in the window even with far more than 40
  tool lines interleaved` — 45 content messages with 10 tool lines between
  every pair (450 tool lines total); asserts exactly `REPLAY_CONTENT_CAP`
  (40) content entries survive and every one of them really is content —
  the direct regression guard the coordinator asked for.
- `never downgrades a message that is both content and tool-bearing into a
  tool line` — the exact shape that broke the e2e test
  (`isContent: true, hasTool: true`); asserts it renders as `"content"`.
- `adds tool lines only from inside the span content already covers,
  capped separately` — tool lines placed BEFORE the first kept content
  message are dropped.
- `preserves original chronological order when merging content and tool
  lines`.

### TDD evidence for round 2

RED — temporarily replaced `selectReplayLines`'s body with the old, naive
"one shared filter + one shared `slice(-40)`, tool line if `hasTool`"
behavior (i.e. checking `hasTool` before `isContent`, mirroring the actual
round-1 bug) and ran the new tests:

```
$ pnpm vitest run server/turn-context.test.ts
 Test Files  1 failed (1)
      Tests  3 failed | 21 passed (24)
```

The 3 failures were exactly the 3 new regression tests; all 21 pre-existing
`turn-context.test.ts` tests kept passing. Restored the real implementation
— GREEN, same command: `Test Files 1 passed (1)`, `Tests 24 passed (24)`.

Separately confirmed the actual e2e regression is fixed:

```
$ pnpm vitest run server/direct-coordination.e2e.test.ts
 Test Files  1 passed (1)
      Tests  10 passed (10)
```

(This was RED — 1 failed / 9 passed, the exact assertion from the
coordinator's report — against the round-1 code before this round's fix;
confirmed by running it unmodified before making any round-2 change.)

### Final gate for round 2 (the wider set, run in order)

```
$ pnpm vitest run server/quota-switch.test.ts server/turn-context.test.ts server/peer-approval.test.ts server/direct-coordination.e2e.test.ts server/agent-tool-policy.test.ts
 Test Files  5 passed (5)
      Tests  91 passed (91)

$ npx tsc -p tsconfig.server.json --noEmit
(no output, exit 0)

$ pnpm lint
(no output, exit 0)
```

`server/comms.test.ts` was not run, per instruction (fails 15/25 on current
main for unrelated reasons).

### Files changed in round 2

- `server/turn-context.ts` — new exported `selectReplayLines`,
  `ReplayCandidate`, `REPLAY_CONTENT_CAP`, `REPLAY_TOOL_LINE_CAP`.
- `server/turn-context.test.ts` — new `describe("selectReplayLines", ...)`
  block (4 tests, listed above).
- `server/index.ts` — `startTurn`'s `replayTranscript` construction now
  delegates to `selectReplayLines` instead of the inline round-1 filter/cap
  logic; comment rewritten to state the real invariant (content capped at
  40, tool lines capped separately, so tool lines cannot displace content
  — and the double-classification pitfall that actually broke it).

### Self-review

- Verified the fix against the ACTUAL failing assertion (not just against
  my own restated theory of the bug) before declaring it fixed — the first
  attempt at "cap them separately" did not, in fact, fix the e2e test, and
  I did not stop at "the diff looks like what was asked for."
- The extraction into `turn-context.ts` was not requested explicitly, but
  the task's own gate ("add a unit test... proving...") is not satisfiable
  as a *regression guard* against logic that lives only inline in
  `server/index.ts` (which has no unit-test harness for `startTurn`) — so a
  test written against a reimplementation in the test file would drift
  from the real code and stop guarding anything. Extracting the actual
  algorithm into a pure function keeps the test honest.
- Did not touch the duplicated `transcript`/`replayTranscript`-adjacent
  code any further than required, and did not add boot-time stale-card
  cleanup — both remain explicitly deferred, per instruction.

### Concerns

- `selectReplayLines`'s tool-line window starts at the first KEPT content
  message's original index and runs to the end of the eligible array
  (there is no explicit upper bound distinct from "the end of the
  messages") — this matches "fall inside that window" as I read it (the
  window content already spans, all the way to now), but if a narrower
  reading was intended (e.g. bounded above by the last content message,
  excluding trailing tool-only activity after it) that would be a small
  follow-up change confined to this one function.
