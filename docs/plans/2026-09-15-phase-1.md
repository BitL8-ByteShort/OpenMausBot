# Phase 1 — context discipline

Status: part 1 built on branch `phase-1/long-threads` (Sep 15, 2026). Second phase of the
harness programme (`../../../harness-gap-analysis.md` §17). Phase 1 as written there is a long
list; it is built in measured parts, each with its own spec and plan:

| Part | Scope | Spec / plan | Metric |
| --- | --- | --- | --- |
| 1 · long threads | harness-owned compaction: budget per task, deterministic record, fresh session on a budgeted replay | `docs/superpowers/specs/2026-09-15-phase-1-long-threads-design.md`, `docs/superpowers/plans/2026-09-15-phase-1-long-threads.md` | scorecard T5: input at turn 10, total over ten turns |
| 2 · recall | memory index queried at turn start with a disclosure chip; a bot recalls its own room work | to write | a "told X in one thread, uses X in another" task |
| 3 · prefix fixes | F3 (skills out of the stable half), per-thread paths out of the system prompt | to write | first-turn cache write per new thread |

## Why this order

The audit on Sep 15 (`docs/bench/scorecard/2026-09-15-main-vs-phase0.md` and the prompt audit in
part 1's spec) showed that per Claude call about 43,000 input tokens are Claude Code's own prompt
and ours is about 1,300; follow-up turns are served almost entirely from cache and a new thread
already reuses about 75% of the prefix. Trimming our prompt cannot move the numbers much; long
threads and recall can.

## PRs checked before building (AGENTS.md rule 2)

- #759 (closed, "close and split"): budget-as-share-of-window, model-window table and the
  compaction prompt lifted; character estimates replaced by reported tokens.
- #760 (closed): a resumed turn never compacts; the harness forces the rebuild.
- #1080 (closed): hard cap forcing compact plus host reset, per-turn notebook page; the digest is
  our page.
- #1184 (open, this account): its replay tool-history half is superseded by Phase 0 digests; the
  quota-switch card stands and needs a rebase onto #1243.
- #59 (open, conflicting): per-task execution limits; different feature, shares only the word.

## Findings while building

- **F6 (fixtures):** the Codex fake honours `thread/resume` only in `FAKE_CODEX_MODE=resume`; in
  the default mode any second turn on a thread stalls for 30 s, on main as well. Tests that run
  more than one turn on the Codex fake must set that mode (the compaction e2e does). Worth a
  fixture fix in its own PR.
